<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- Warna background transparan -->
    <stroke
        android:width="10dp"
        android:color="@color/primary" /> <!-- Warna border -->
    <corners android:radius="14dp" /> <!-- Sesuaikan dengan sudut CardView -->
</shape>